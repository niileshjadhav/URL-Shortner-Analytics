version: '1.0'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER:postgres
      POSTGRES_PASSWORD:root
      POSTGRES_DB:url_shortener
    ports:
      - "5432:5432"

  redis:
    image: redis:7
    ports:
      - "6379:6379"

